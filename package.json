{
  "name": "web-fe-lint",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "lerna clean && rm -rf node_modules",
    "test": "lerna run test",
    "publish": "lerna publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.6"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
